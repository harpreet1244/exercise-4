
x = float(input("whats x ? "))
y = float(input("whats y ? "))
z = round(x + y)
print(z)

# just to put a comma to make it money number
x = float(input("whats x ? "))
y = float(input("whats y ? "))
z = round(x + y)
print(f"{z: ,}")

x = float(input("whats x ? "))
y = float(input("whats y ? "))
z = x / y
print(f"{z:.2f}")
